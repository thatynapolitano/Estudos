
import random
valorDado = random.randint(1,6) # Um dado, por exemplo, pode gerar valores aleatorios de 1 a 6. (random é a biblioteca, .randint é a função que recebe números inteiros)
temperatura = random.uniform(35.5, 41.9) # .uniform é a função que recebe valores flutuantes.
